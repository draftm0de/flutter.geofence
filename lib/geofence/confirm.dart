/// Configuration passed to the notifier when an exit confirmation is required.
class DraftModeGeofenceConfirm {
  final String title;
  final String message;
  DraftModeGeofenceConfirm({
    required this.title,
    required this.message
  });
}
